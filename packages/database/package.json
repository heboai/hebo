{
  "name": "@hebo/database",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate",
    "clean": "git clean -fdx .",
    "db": "DATABASE_URL=postgresql://postgres:password@localhost:5432/local prisma",
    "format": "prisma format"
  },
  "devDependencies": {
    "bun-types": "^1.2.22",
    "prisma": "^6.17.0",
    "prismabox": "^1.1.25"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.17.0",
    "@prisma/client": "^6.17.0",
    "crypto-random-string": "^5.0.0",
    "slugify": "^1.6.6"
  }
}
