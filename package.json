{
  "name": "hebo",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "infra"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "db:generate": "pnpm --filter @hebo/db exec sst shell drizzle-kit generate --config ../../sst.config.ts",
    "db:migrate": "pnpm --filter @hebo/db exec sst shell drizzle-kit migrate --config ../../sst.config.ts",
    "db:push": "pnpm --filter @hebo/db exec sst shell drizzle-kit push --config ../../sst.config.ts",
    "db:studio": "pnpm --filter @hebo/db exec sst shell drizzle-kit studio --config ../../sst.config.ts",
    "deploy": "turbo run deploy",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@hebo/db": "workspace:*",
    "@types/aws-lambda": "8.10.150",
    "eslint": "^9",
    "prettier": "^3.2.5",
    "sst": "3.17.10",
    "turbo": "^1.12.4",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
