import { UserName } from "~/components/auth/UserName";
import { Logo } from "~/components/ui/Logo";
import { GenerateApiKey } from "~/components/ui/GenerateApiKey";

<Logo className="hidden md:flex" />

**Hi <UserName />!**

Start using our playground or code your first agent.

## Step 1: Create your first API key

<GenerateApiKey />
Copy your key once generated, for security reasons you won’t be able to see it
again.

## Step 2: Connect using your favorite framework

First, install the required package:

<CodeGroup>
```bash Vercel AI SDK
$ npm install ai
```

```bash Langchain
$ pip install -U langchain-openai portkey-ai
```

</CodeGroup>

Then, create the following code:

<CodeGroup>
```ts Vercel AI SDK
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';
import { generateText } from 'ai';

const provider = createOpenAICompatible({
apiKey: 'YOUR_API_KEY',
baseURL: 'https://cloud.hebo.ai/api',
});

const response = await generateText(
model: provider('YOUR_MODEL_ALIAS'),
{ prompt: 'Tell me a joke about AI.' }
);
console.log(result);

````

```python Langchain
from langchain_openai import ChatOpenAI
import os

portkey_headers = createHeaders(
    api_key="YOUR_API_KEY",
    provider="openai"
)

llm = ChatOpenAI(
    base_url="https://cloud.hebo.ai/api",
    model="YOUR_MODEL_ALIAS"
)

response = llm.invoke("Tell me a joke about AI.")
print(response.content)
````

</CodeGroup>

## Step 3: Use your evaluations & observability tools as usual

Continue to read our full documentation to [learn more](https://docs.hebo.ai/hebo_eval)
