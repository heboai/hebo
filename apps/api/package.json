{
  "name": "@hebo/api",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "echo 'Build completed' > .build-complete",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "clean": "rimraf .next .turbo node_modules dist .build-complete"
  },
  "devDependencies": {
    "@types/node": "*",
    "esbuild": "*"
  },
  "dependencies": {
    "@hebo/db": "workspace:*",
    "hono": "^4.8.4",
    "jose": "^6.0.12"
  }
}
