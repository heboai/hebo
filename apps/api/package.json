{
  "name": "@hebo/api",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "echo 'No build step needed - using Bun'",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test:migration": "bun test-migration.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.4",
    "npm-run-all2": "^6.2.0"
  },
  "dependencies": {
    "hono": "^4.8.4",
    "@hebo/db": "workspace:*"
  }
}
