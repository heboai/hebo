{
  "name": "@hebo/api",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "echo 'No build step needed - using Bun'",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "clean": "rimraf .next .turbo node_modules dist"
  },
  "devDependencies": {
    "@types/node": "*",
    "esbuild": "*",
    "npm-run-all2": "*"
  },
  "dependencies": {
    "hono": "*",
    "@hebo/db": "workspace:*"
  }
}
